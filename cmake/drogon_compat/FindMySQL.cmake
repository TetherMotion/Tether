# Stub FindMySQL.cmake for systems where Drogon's Debian package requires
# MySQL but it is not installed. Tether's HTTP server does not use MySQL.
set(MySQL_FOUND TRUE)
set(MySQL_LIBRARIES "")
set(MySQL_INCLUDE_DIRS "")
